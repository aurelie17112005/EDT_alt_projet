{
  "name": "mon-projet-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "install:all": "npm run install:frontend && npm run install:backend",
    "build:frontend": "cd frontend && npm run build",
    "copy:frontend": "rm -rf backend/public && cp -r frontend/dist backend/public",
    "build": "npm run build:frontend && npm run copy:frontend",
    "fullbuild": "npm run install:frontend && npm run build && npm run install:backend",
    "start": "node backend/server.js"
  }
}
